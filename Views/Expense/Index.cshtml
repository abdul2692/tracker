@model IEnumerable<SpendingTracker.Models.Expense>
@{
    ViewData["Title"] = "Expenses";
    int page = ViewBag.Page;
    int totalPages = ViewBag.TotalPages;
    var categories = ViewBag.Categories as IEnumerable<SpendingTracker.Models.Category> ?? Enumerable.Empty<SpendingTracker.Models.Category>();
}

@{
    int curMonth = ViewBag.SelectedMonth ?? DateTime.Now.Month;
    int curYear = ViewBag.SelectedYear ?? DateTime.Now.Year;
    var selectedMonthName = System.Globalization.CultureInfo.CurrentCulture.DateTimeFormat.GetMonthName(curMonth);
}

<div class="page-header d-flex flex-wrap justify-content-between align-items-center gap-3">
    <div>
        <h1 class="page-title">Expenses</h1>
        <p class="page-subtitle">Total: <strong class="text-danger">@ViewBag.CurrencySymbol@ViewBag.TotalExpenses.ToString("N2")</strong> — @selectedMonthName @curYear (@ViewBag.TotalCount records)</p>
    </div>
    <a asp-action="Create" class="btn btn-danger-custom">
        <i class="bi bi-plus-circle me-1"></i>Add Expense
    </a>
</div>

<!-- Filters -->
<div class="card-custom mb-4">
    <div class="card-custom-body py-3">
        <form method="get" class="row g-2 align-items-end">
            <div class="col-6 col-md-2">
                <label class="form-label-custom small mb-1">Month</label>
                <select name="month" class="form-select form-control-dark">
                    @for (int m = 1; m <= 12; m++)
                    {
                        <option value="@m" selected="@(m == curMonth)">@System.Globalization.CultureInfo.CurrentCulture.DateTimeFormat.GetMonthName(m)</option>
                    }
                </select>
            </div>
            <div class="col-6 col-md-2">
                <label class="form-label-custom small mb-1">Year</label>
                <select name="year" class="form-select form-control-dark">
                    @for (int y = DateTime.Now.Year - 3; y <= DateTime.Now.Year + 1; y++)
                    {
                        <option value="@y" selected="@(y == curYear)">@y</option>
                    }
                </select>
            </div>
            <div class="col-12 col-md-3">
                <label class="form-label-custom small mb-1">Search</label>
                <div class="input-group-custom">
                    <i class="bi bi-search input-icon"></i>
                    <input type="text" name="search" value="@ViewBag.Search" class="form-control-custom" placeholder="Search..." />
                </div>
            </div>
            <div class="col-6 col-md-2">
                <label class="form-label-custom small mb-1">Category</label>
                <select name="categoryId" class="form-select form-control-dark">
                    <option value="">All Categories</option>
                    @foreach (var cat in categories)
                    {
                        <option value="@cat.Id" selected="@(ViewBag.CategoryId != null && (int)ViewBag.CategoryId == cat.Id)">@cat.Name</option>
                    }
                </select>
            </div>
            <div class="col-6 col-md-2">
                <label class="form-label-custom small mb-1">Sort</label>
                <select name="sortBy" class="form-select form-control-dark">
                    <option value="">Latest First</option>
                    <option value="date_asc" selected="@(ViewBag.SortBy == "date_asc")">Oldest First</option>
                    <option value="amount_desc" selected="@(ViewBag.SortBy == "amount_desc")">Highest Amount</option>
                    <option value="amount_asc" selected="@(ViewBag.SortBy == "amount_asc")">Lowest Amount</option>
                    <option value="category" selected="@(ViewBag.SortBy == "category")">Category</option>
                </select>
            </div>
            <div class="col-12 col-md-1">
                <button type="submit" class="btn btn-primary-custom w-100">Filter</button>
            </div>
        </form>
    </div>
</div>

<div class="card-custom">
    <div class="card-custom-body p-0">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-custom mb-0">
                    <thead>
                        <tr>
                            <th>Description</th>
                            <th>Category</th>
                            <th>Payment</th>
                            <th>Date</th>
                            <th class="text-end">Amount</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var expense in Model)
                        {
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="icon-circle me-2" style="background:@(expense.Category.Color)22">
                                            <i class="@expense.Category.Icon" style="color:@expense.Category.Color"></i>
                                        </div>
                                        <span class="fw-500">@(expense.Description ?? expense.Category.Name)</span>
                                    </div>
                                </td>
                                <td>
                                    <span class="badge-category" style="background:@(expense.Category.Color)22;color:@expense.Category.Color">
                                        @expense.Category.Name
                                    </span>
                                </td>
                                <td class="text-muted small">@expense.PaymentMethod</td>
                                <td class="text-muted">@expense.Date.ToString("MMM dd, yyyy")</td>
                                <td class="text-end fw-600 text-danger">-@ViewBag.CurrencySymbol@expense.Amount.ToString("N2")</td>
                                <td class="text-center">
                                    <a asp-action="Edit" asp-route-id="@expense.Id" class="btn-icon btn-icon-edit" title="Edit">
                                        <i class="bi bi-pencil-fill"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@expense.Id" class="btn-icon btn-icon-delete" title="Delete">
                                        <i class="bi bi-trash-fill"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (totalPages > 1)
            {
                <div class="pagination-wrapper">
                    <nav>
                        <ul class="pagination-custom">
                            @if (page > 1)
                            {
                                <li><a class="page-btn" asp-action="Index" asp-route-page="@(page-1)" asp-route-search="@ViewBag.Search" asp-route-sortBy="@ViewBag.SortBy" asp-route-categoryId="@ViewBag.CategoryId" asp-route-month="@curMonth" asp-route-year="@curYear"><i class="bi bi-chevron-left"></i></a></li>
                            }
                            @for (int i = Math.Max(1, page - 2); i <= Math.Min(totalPages, page + 2); i++)
                            {
                                var btnCls = i == page ? "page-btn active" : "page-btn";
                                <li><a class="@btnCls" asp-action="Index" asp-route-page="@i" asp-route-search="@ViewBag.Search" asp-route-sortBy="@ViewBag.SortBy" asp-route-categoryId="@ViewBag.CategoryId" asp-route-month="@curMonth" asp-route-year="@curYear">@i</a></li>
                            }
                            @if (page < totalPages)
                            {
                                <li><a class="page-btn" asp-action="Index" asp-route-page="@(page+1)" asp-route-search="@ViewBag.Search" asp-route-sortBy="@ViewBag.SortBy" asp-route-categoryId="@ViewBag.CategoryId" asp-route-month="@curMonth" asp-route-year="@curYear"><i class="bi bi-chevron-right"></i></a></li>
                            }
                        </ul>
                    </nav>
                    <span class="text-muted small">Page @(page) of @totalPages</span>
                </div>
            }
        }
        else
        {
            <div class="empty-state py-5">
                <i class="bi bi-receipt empty-icon fs-1 text-muted mb-3 d-block"></i>
                <h5 class="fw-600">No expenses recorded for this month.</h5>
                <p class="text-muted small">Start tracking your spending by adding an expense for @selectedMonthName @curYear.</p>
                <a asp-action="Create" class="btn btn-danger-custom mt-2"><i class="bi bi-plus-circle me-1"></i>Add Expense</a>
            </div>
        }
    </div>
</div>

